import SwiftUI

struct TasksTab: View {
    var body: some View {
        PlaceholderTab(
            symbol: "checklist",
            title: "Tasks",
            detail: "A board with statuses you can drag between, arriving in M2."
        )
    }
}
