import SwiftUI

struct NotesTab: View {
    var body: some View {
        PlaceholderTab(
            symbol: "doc.text",
            title: "Notes",
            detail: "Multiple notes as tabs, arriving in M1."
        )
    }
}
